module.exports = function(horaInicial, horaFinal){
  if (horaInicial + horaFinal <= 24) {
    horaFinal *= 3600;
    horaInicial *= 3600;
    return duracaoXadrez = (horaFinal + horaInicial) / 3600;
  } return (err)
}
